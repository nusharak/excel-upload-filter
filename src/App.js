import React from 'react';
import { Container } from '@mui/material';
import UploadExcel from './UploadExcel';

const App = () => {
  return (
    <Container>
      <UploadExcel />
    </Container>
  );
};

export default App;
